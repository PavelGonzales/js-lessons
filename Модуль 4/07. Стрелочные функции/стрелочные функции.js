const fn = (params) => {
  // какое-то действие
}

function pow(number) {
  return number * number;
}

const newPow = (number) => {
  console.log(number * number);
}

const newPowOneLine = (number) => number * number; // показать в коллбеках массивах 